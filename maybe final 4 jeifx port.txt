<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Jefix.Design — Premium Thumbnail Designer</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800;900&display=swap" rel="stylesheet">
  <style>
    :root {
      --black: #0a0a0a;
      --purple1: #6a0dad;
      --purple2: #b57fff;
      --glass: rgba(255,255,255,0.06);
      --border: rgba(255,255,255,0.1);
      --text: rgba(255,255,255,0.95);
    }

    * {margin:0;padding:0;box-sizing:border-box;}
    body {
      font-family: Inter, sans-serif;
      color: var(--text);
      background: radial-gradient(circle at 30% 30%, #14021f, #000 60%);
      overflow-x: hidden;
    }
    a {color:inherit;text-decoration:none;}

    /* MOVING ORBS */
    .orb {
      position: fixed;
      width: 300px;
      height: 300px;
      border-radius: 50%;
      filter: blur(100px);
      opacity: 0.5;
      z-index: 0;
      animation: float 20s infinite alternate ease-in-out;
    }
    .orb1 {background: #6a0dad; top: 10%; left: -10%;}
    .orb2 {background: #b57fff; bottom: 10%; right: -10%; animation-delay: 5s;}
    @keyframes float {
      0% {transform: translate(0,0) scale(1);}
      100% {transform: translate(50px,50px) scale(1.2);}
    }

    /* NAVBAR */
    nav {
      position: fixed; top:20px; left:20px; right:20px; z-index: 10;
      display:flex;justify-content:space-between;align-items:center;
    }
    .brand {font-weight:900;font-size:24px;}
    .nav-links {display:flex;gap:20px;}
    .nav-links a {padding:8px 14px;border-radius:10px;transition:.3s;}
    .nav-links a:hover {background:var(--glass);border:1px solid var(--border);}

    /* MAIN LAYOUT */
    main.page {padding:120px 28px 60px;max-width:1200px;margin:0 auto;}
    .container {display:grid;grid-template-columns:1fr 420px;gap:36px;align-items:center;}
    @media(max-width:900px){.container{grid-template-columns:1fr}}

    h1 {font-size:48px;font-weight:900;line-height:1.1;}
    h2 {font-size:32px;font-weight:800;margin-bottom:14px;}
    .accent {background:linear-gradient(90deg,var(--purple1),var(--purple2));-webkit-background-clip:text;color:transparent;}
    .glow {color:var(--purple2);text-shadow:0 0 12px var(--purple2);}

    /* Cards */
    .subglass {
      margin-top:14px;background:var(--glass);
      border:1px solid var(--border);padding:18px;border-radius:14px;
    }
    .portrait-wrap {
      width:420px;max-width:86vw;background:var(--glass);
      border:1px solid var(--border);padding:14px;border-radius:20px;
      box-shadow:0 30px 80px rgba(0,0,0,.7);
    }
    .portrait {border-radius:12px;width:100%;}

    /* ABOUT */
    .about-section {margin:50px 0;}
    .about-section .subglass {font-size:18px;line-height:1.6;}

    /* HIRE BUTTON */
    .float-hire {
      position:fixed;right:28px;bottom:28px;z-index:50;
      background:linear-gradient(90deg,var(--purple1),var(--purple2));
      padding:14px 22px;border-radius:999px;border:none;color:white;
      font-weight:800;cursor:pointer;box-shadow:0 8px 40px rgba(124,58,237,.4);
    }

    /* WORK */
    .thumb {width:220px;height:140px;border-radius:12px;overflow:hidden;background:var(--glass);border:1px solid var(--border);flex:0 0 auto;}
    .thumb img {width:100%;height:100%;object-fit:cover;}
    .marquee {overflow:hidden;margin-top:14px;}
    .track {display:flex;gap:18px;width:max-content;}
    .mar-left .track {animation:scrollLeft 20s linear infinite;}
    .mar-right .track {animation:scrollRight 22s linear infinite;}
    @keyframes scrollLeft {0%{transform:translateX(0)}100%{transform:translateX(-50%)}}
    @keyframes scrollRight {0%{transform:translateX(-50%)}100%{transform:translateX(0)}}

    /* INQUIRY PANEL */
    .inquiry-panel {
      position:fixed;top:0;right:-100%;width:100%;max-width:650px;height:100%;
      background:rgba(10,10,15,0.98);z-index:100;transition:.5s;padding:40px;overflow-y:auto;
    }
    .inquiry-panel.show {right:0;}
    .close-btn {position:absolute;top:20px;right:20px;font-size:28px;background:none;border:none;color:white;cursor:pointer;}
    label {display:block;margin-top:14px;font-weight:600;}
    input,textarea {
      width:100%;padding:12px;margin-top:6px;border-radius:8px;
      border:1px solid var(--border);background:rgba(255,255,255,0.05);color:white;font-size:16px;
    }
    textarea {min-height:100px;}

    /* Glassy Select Rows */
    .glass-options {
      display:flex;flex-wrap:wrap;gap:10px;margin-top:10px;
    }
    .glass-options span {
      flex:1 1 calc(50% - 10px);text-align:center;padding:12px;border-radius:10px;
      background:linear-gradient(145deg,#000,#1a1a1a);border:1px solid var(--border);
      color:white;cursor:pointer;transition:.3s;box-shadow:inset 0 0 10px rgba(255,255,255,0.1),0 0 10px rgba(124,58,237,0.2);
    }
    .glass-options span:hover {background:rgba(124,58,237,0.2);}
    .glass-options span.selected {border:1px solid var(--purple2);box-shadow:0 0 15px rgba(124,58,237,.5);}

    button.send {
      margin-top:20px;padding:12px;border:none;width:100%;font-weight:800;
      background:linear-gradient(90deg,var(--purple1),var(--purple2));
      border-radius:8px;color:white;cursor:pointer;
    }

    /* FOOTER */
    footer {text-align:center;margin-top:60px;padding:40px 0;}
    .socials {display:flex;justify-content:center;gap:20px;margin-top:12px;}
    .socials a {padding:10px 18px;border-radius:12px;font-weight:700;background:linear-gradient(90deg,var(--purple1),var(--purple2));color:white;text-shadow:0 0 8px rgba(255,255,255,0.6);}

    /* THANK YOU PAGE */
    .thankyou-page {
      display:none;position:fixed;top:0;left:0;width:100%;height:100%;
      background:#0a0a0a;z-index:200;display:flex;flex-direction:column;justify-content:center;align-items:center;text-align:center;padding:20px;
    }
    .thankyou-page h1 {font-size:70px;font-weight:900;color:white;text-shadow:0 0 20px var(--purple2);}
    .thankyou-page p {font-size:20px;margin-top:10px;}
    .thankyou-page .dm {font-size:32px;font-weight:900;color:var(--purple2);margin:20px 0;text-shadow:0 0 15px var(--purple2);}
    .thankyou-page .socials a {font-size:18px;}
  </style>
</head>
<body>
  <div class="orb orb1"></div>
  <div class="orb orb2"></div>

  <!-- NAVBAR -->
  <nav>
    <div class="brand">jefix<span style="opacity:.7">.</span>design</div>
    <div class="nav-links">
      <a href="#work">Work</a>
      <a href="#about">About</a>
      <a id="hireLink">Hire</a>
    </div>
  </nav>

  <!-- MAIN -->
  <main class="page">
    <!-- INTRO -->
    <div class="container">
      <div>
        <h1>Hey — it's <span class="accent">Jeff</span></h1>
        <div class="subglass">
          <strong>I'M A <span class="glow">THUMBNAIL DESIGNER</span></strong>
          <p>I create <span class="glow">SCROLL-STOPPING</span> thumbnails that <span class="glow">BOOST CTR</span> & <span class="glow">GROW CHANNELS</span>.</p>
        </div>
      </div>
      <div class="portrait-wrap"><img src="profile.png" class="portrait" alt="Jeff portrait"></div>
    </div>

    <!-- ABOUT -->
    <section id="about" class="about-section">
      <h2>About</h2>
      <div class="subglass">
        <p>I focus on <strong>high-CTR strategies</strong> with bold visuals and strong color psychology. My mission is to craft thumbnails that <span class="glow">demand attention</span>, bring traffic, and help creators succeed.</p>
      </div>
    </section>

    <!-- WORK -->
    <section id="work" class="work-section">
      <h2>Selected Work</h2>
      <div class="marquee mar-left"><div class="track" id="rowTop"></div></div>
      <div class="marquee mar-right"><div class="track" id="rowBottom"></div></div>
    </section>

    <!-- FOOTER -->
    <footer>
      <h2>Contact Me</h2>
      <div class="socials">
        <a href="mailto:jefix.designs@gmail.com">📧 Email</a>
        <a href="https://instagram.com/jefix.designs" target="_blank">📸 Instagram</a>
      </div>
    </footer>
  </main>

  <!-- HIRE BUTTON -->
  <button class="float-hire" id="floatHire">Hire Me</button>

  <!-- INQUIRY PANEL -->
  <div class="inquiry-panel" id="inquiryPanel">
    <button class="close-btn" id="closePanel">&times;</button>
    <h2>Project Inquiry</h2>
    <form id="whatsappForm">
      <label>Your Name*</label><input name="name" required>
      <label>Age*</label><input name="age" type="number" required>
      <label>Country*</label><input name="country" required>
      <label>Why do you need a thumbnail designer?</label><textarea name="reason"></textarea>
      <label>Phone/WhatsApp*</label><input name="phone" required>
      <label>Channel Link</label><input name="channel">
      <label>Instagram ID</label><input name="instagram">
      <label>Email ID*</label><input name="email" type="email" required>

      <!-- Budget Glass Options -->
      <label>Budget</label>
      <div class="glass-options" id="budgetOptions">
        <span>$10/100</span><span>$500/1000</span>
        <span>$1000/2500</span><span>$2500/5000</span>
      </div>

      <!-- Thumbnail Types -->
      <label>Thumbnail Type</label>
      <div class="glass-options" id="typeOptions">
        <span>Gaming</span><span>Documentary</span>
        <span>Blogs</span><span>Challenges</span>
        <span>Business</span><span>Money Strategy</span>
        <span>Comedy</span>
      </div>

      <label>Personal Note to Jefix.Designs</label><textarea name="note"></textarea>
      <button class="send" type="submit">Send via WhatsApp</button>
    </form>
  </div>

  <!-- THANK YOU PAGE -->
  <div class="thankyou-page" id="thankyouPage">
    <h1>THANKS</h1>
    <p>Let's work together and grow your CTR!</p>
    <div class="dm">DM ME</div>
    <div class="socials">
      <a href="mailto:jefix.designs@gmail.com">📧 Email</a>
      <a href="https://instagram.com/jefix.designs" target="_blank">📸 Instagram</a>
    </div>
  </div>

  <script>
    // THUMBNAILS
    const thumbs=['1','2','3','4','5'].map(n=>`https://picsum.photos/seed/${n}/800/600`);
    function addThumbs(id){const track=document.getElementById(id);thumbs.concat(thumbs).forEach(src=>{const d=document.createElement('div');d.className='thumb';d.innerHTML=`<img src="${src}">`;track.appendChild(d);});}
    addThumbs('rowTop');addThumbs('rowBottom');

    // PANEL TOGGLE
    const panel=document.getElementById("inquiryPanel");
    document.getElementById("hireLink").onclick=()=>panel.classList.add("show");
    document.getElementById("floatHire").onclick=()=>panel.classList.add("show");
    document.getElementById("closePanel").onclick=()=>panel.classList.remove("show");

    // SELECT HANDLING
    function handleGlassSelection(groupId){
      const container=document.getElementById(groupId);
      container.querySelectorAll("span").forEach(span=>{
        span.onclick=()=>{
          container.querySelectorAll("span").forEach(s=>s.classList.remove("selected"));
          span.classList.add("selected");
          container.setAttribute("data-value",span.innerText);
        }
      });
    }
    handleGlassSelection("budgetOptions");
    handleGlassSelection("typeOptions");

    // FORM SUBMIT
    document.getElementById("whatsappForm").addEventListener("submit",function(e){
      e.preventDefault();
      const data=new FormData(this);
      const budget=document.getElementById("budgetOptions").getAttribute("data-value")||"Not selected";
      const type=document.getElementById("typeOptions").getAttribute("data-value")||"Not selected";
      let msg="📩 *New Inquiry*%0A";
      msg+=`👤 Name: ${data.get("name")}%0A🎂 Age: ${data.get("age")}%0A🌍 Country: ${data.get("country")}%0A🤔 Reason: ${data.get("reason")}%0A📞 Phone: ${data.get("phone")}%0A📺 Channel: ${data.get("channel")}%0A📸 Instagram: ${data.get("instagram")}%0A📧 Email: ${data.get("email")}%0A💰 Budget: ${budget}%0A🎨 Type: ${type}%0A📝 Note: ${data.get("note")}%0A`;
      const url=`https://wa.me/1234567890?text=${msg}`;
      window.open(url,"_blank");
      panel.classList.remove("show");

      // Show Thank You Page
      setTimeout(()=>document.getElementById("thankyouPage").style.display="flex",400);
    });
  </script>
</body>
</html>
